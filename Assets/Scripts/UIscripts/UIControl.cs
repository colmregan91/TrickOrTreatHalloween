using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIControl : MonoBehaviour
{
    //public Camera[] UICameras;

    //private IEnumerator Start()
    //{
    //    yield return new WaitUntil(() => CameraSetUp.camsInitialized == true);

    //    addUIforPlayers(CameraSetUp.playerCameras.Count);
    //}


    //void addUIforPlayers(int playerNumber)
    //{
    //    Camera player1Camera = UICameras[0];
    //    Camera player2Camera = UICameras[1];
    //    //Camera player3Camera = UICameras[2];
    //    //Camera player4Camera = UICameras[3];

    //    switch (playerNumber)
    //    {
    //        case 1:
    //            player1Camera.gameObject.SetActive(true);
    //            player1Camera.rect = new Rect(0, 0, 1, 1);
    //            player1Camera.depth = 0;
    //            break;
    //        case 2:
    //            player1Camera.gameObject.SetActive(true);
    //            player1Camera.rect = new Rect(0, 0.5f, 1, 1);
    //            player1Camera.depth = 0;
    //            player2Camera.gameObject.SetActive(true);
    //            player2Camera.rect = new Rect(0, -0.5f, 1, 1);
    //            player2Camera.depth = 1;
    //            break;

    //    }
    //}
}
