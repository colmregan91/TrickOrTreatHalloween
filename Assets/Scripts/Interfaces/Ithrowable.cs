using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface Ithrowable 
{
    // void Throw(Vector3 dir,Vector3 StartPos, Pool Whichpool);

    //   GameObject SetMyPool(Transform shootpos);

    void Throw(Vector3 dir);
}
